# Client-side (bundled into the static build, safe to expose): Turnstile SITE key.
PUBLIC_TURNSTILE_SITE_KEY=

# Used only by scripts/generate-city-content.mjs (never bundled, never deployed).
ANTHROPIC_API_KEY=
